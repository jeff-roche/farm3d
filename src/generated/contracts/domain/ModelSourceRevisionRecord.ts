// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ImportWarning } from "../command/ImportWarning";
import type { Inspection } from "./Inspection";
import type { InspectionSummary } from "./InspectionSummary";
import type { ModelFormat } from "./ModelFormat";
import type { RevisionOrigin } from "./RevisionOrigin";

/**
 * Spec §Domain types: one revision in full, as `list_model_revisions`
 * returns it — the summary plus where it was read from, the inspector that
 * read it, the whole [`formats::Inspection`], and its warnings.
 * `sourcePath` is the path the revision was captured from, shown in the
 * revision history.
 */
export type ModelSourceRevisionRecord = { sourcePath: string, sourceMtime: string | null, inspectorVersion: number, inspection: Inspection, warnings: Array<ImportWarning>, id: string, modelId: string, sequence: number, sha256: string, sizeBytes: number, format: ModelFormat, origin: RevisionOrigin, sourceFileName: string, capturedAt: string, hasThumbnail: boolean, summary: InspectionSummary, };
